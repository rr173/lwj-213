version: '3.8'

services:
  topology-app:
    build: .
    container_name: topology-editor
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - PORT=3000
      - NODE_ENV=production
    restart: unless-stopped
